# Selected checkpoint

The publishing procedure copies one immutable checkpoint here as:

```text
ppo_route_guided_263443_steps.pt
```

This is the checkpoint evaluated deterministically on 30 unseen Level-2 seeds,
with 25 successes, 5 collisions and no timeouts. Its SHA-256 hash is stored in
`docs/results/checkpoint_sha256.txt` after the copy.
